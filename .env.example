# Copy this file to `.env` and fill in your values.
#   cp .env.example .env   (or copy it in your editor)
# `.env` is git-ignored — never commit your real key. A real environment
# variable, if set, always takes precedence over this file.

# Free key from https://the-odds-api.com/ — used by scripts/fetch_odds.py
# to pull real bookmaker odds into data/odds.csv and data/outright_odds.csv.
ODDS_API_KEY=
