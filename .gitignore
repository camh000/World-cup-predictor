# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.pytest_cache/
.coverage
htmlcov/

# Secrets — local API keys etc. Never commit. See .env.example for the template.
.env
.env.*
!.env.example

# Local Claude Code settings (machine-specific; not shared)
.claude/settings.local.json

# Generated, evolving model state (regenerated by `wcpredict reset`)
state/*.json
